/* @flow */

import React from 'react';

import styles from './LoginWallet.css';

type Props = {
  className?: string,
};

/* eslint-disable max-len */
const Login = ({ className = '' }: Props) => <div>Login</div>;
/* eslint-enable max-len */

export default Login;
