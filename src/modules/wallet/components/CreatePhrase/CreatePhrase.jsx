/* @flow */

import React from 'react';

import styles from './CreatePhrase.css';

const CreatePhrase = () => (
  <section className={`${styles.content}`}>CreatePhrase</section>
);

export default CreatePhrase;
