/* @flow */

import { Map as ImmutableMap } from 'immutable';

// TODO in #580
const taskFeedItemsReducer = (state: ?ImmutableMap<*, *> = ImmutableMap()) =>
  state;

export default taskFeedItemsReducer;
