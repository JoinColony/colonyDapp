import { newToken } from '../Recipient/Recipient';

export const newRecipient = {
  id: undefined,
  recipient: undefined,
  value: [newToken],
  delay: { amount: undefined, time: 'hours' },
  isExpanded: true,
};
