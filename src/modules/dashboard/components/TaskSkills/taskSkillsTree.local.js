/* @flow */

/*
 * Skills Tree For the Local Dev Network
 * This is the only skill that works on ganache
 */

import type { ConsumableItem } from '~core/ItemsList/ItemsList';

const taskSkills: ConsumableItem[] = [
  {
    id: 3,
    name: 'Accounting',
  },
];

export default taskSkills;
