export enum Context {
  APOLLO_CLIENT = 'apolloClient',
  COLONY_MANAGER = 'colonyManager',
  ENS_INSTANCE = 'ens',
  IPFS_NODE = 'ipfsNode',
  WALLET = 'wallet',
  APP_LOADING = 'appLoadingState',
}
