/* @flow */

export type ColonyType = {
  address: string,
  avatar: string,
  name: string,
  ensName: string,
  description?: string,
  website?: string,
  guideline?: string,
  version?: string,
  id?: string,
};
