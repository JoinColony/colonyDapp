/* @flow */

import { createContext } from 'react';

const reactContext: Object = createContext();

const { Provider, Consumer } = reactContext;

export { Provider, Consumer };

export default reactContext;
